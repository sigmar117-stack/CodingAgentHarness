# CodingKit — REFLECTION 反思报告

> 本文由本人撰写，AI 辅助润色。

## 一、Superpowers 技能评价

### 最有用的

**Brainstorming。** 这玩意儿最值钱的地方不是"生成文档"，是追问。我一开始的想法特模糊——"做个能让 agent 自己修 bug 的东西"。agent 追着问了四五轮，每轮都在逼我细化："你说的反馈闭环指什么？""修正策略做深，深在哪？""危险操作谁判定？"这些问题让我意识到，我原来想的"让 agent 自己改代码"基本等于什么都没想。brainstorming 结束之后，SPEC 里有了四层机制（校验器→分类器→策略引擎→回灌器），每一层都能编码、能测试。这个转变是 AI 追问逼出来的，不是我一开始就想好的。另外，brainstorming 还帮我预判了几个风险，比如 ChromaDB 的复杂度问题、跨平台钥匙串兼容性——这些如果等实现时才发现，会很被动。

**Subagent-driven development + worktrees。** 冷启动阶段用了一个全新的 agent 做 T1.1-T2.1，结果超出预期——它搞定了 4 个 task（我本来以为能完成 1-2 个就不错了），51 个测试全部通过。后面的 T2.2、T2.3、T3.1 也各自在独立 worktree 里由 subagent 完成，我没碰一行代码。这件事让我真正相信了"spec 够清晰的时候，agent 确实能自己推进"。

**TDD。** 不是因为我喜欢 TDD——说实话写测试挺烦的。但在 AI 协作的场景下，测试是唯一能让 agent 知道自己错了的信号。没有测试的话，agent 写了一段代码，看起来挺对，跑起来是错的——agent 不知道，你也不知道，直到上线才发现。红→绿→重构，这个流程给了 agent 一个客观的"完成"标准。

### 形式大于实质的

**Writing-plans。** 不是说 plan 没用。问题是 plan 刚写完就过时了。task 拆分在刚开始的时候是准的，但 agent 一开始执行，发现某些 task 比想象中复杂、某些依赖需要调整，plan 就变成了一篇需要不断更新的文档。到项目后期，我花在更新 PLAN.md 上的时间比写代码还多。plan 更重要的作用可能是在设计阶段帮你理清思路，而不是在执行阶段当路线图。

**Finishing-a-development-branch。** 这个技能的逻辑是"task 完成后决定 merge 还是 discard"。但实际做下来，所有 worktree 最后都 merge 了——因为每个 task 在 plan 阶段已经确定了要做什么。除非 subagent 完全跑偏，否则不存在"要不要 merge"的决策。这个技能在多人协作的 CI/CD 流程里可能更有用，单人项目里有点多余。

## 二、TDD 是阻碍还是放大器？

放大器，但前提是你接受"先写测试"这个节奏。

传统开发里 TDD 让我觉得啰嗦——明明知道要写什么，为什么还要先写一个会挂的测试？但在 AI 协作下，情况反过来了。每次我让 agent 写代码，最怕它写一段"看起来对但实际上错"的东西。TDD 解决了这个问题：先写测试，测试定义了"正确"的标准，agent 的目标就是让测试变绿。

但有个坑：测试本身也会错。有一次 agent 写的测试就有 bug。测试红了，agent 以为是实现代码的问题，改了一轮又一轮，实际上该修的是测试。教训是：人工 review 测试是必要的，不能完全信 agent 写的测试。

## 三、Subagent 能自主运行多久？

项目里最长的一次是冷启动 subagent，连续完成了 4 个 task（T1.1-T2.1），大概跑了 2 小时，51 个测试全过，没跑偏。当时我就在旁边看着，它自己建目录、配文件、装依赖、写代码、跑测试，一条龙搞定了。那个时刻确实有点震撼——原来 spec 写清楚了，agent 真的可以自己干活。

但其他 subagent 没那么顺利。T2.3 的 subagent 擅自改了 PLAN.md 的格式，内容是对的，但格式不一致，后面得人工整理。这事不大，但让我意识到一个问题：agent 在"边界外"的行为你管不住。你让它做 A，它顺手做了 B，B 可能对也可能错。T3.1 的 subagent 在 worktree 上做完了实现，merge 的时候发现 main 已经被其他 task 更新了，有合并冲突。这个不算 agent 的错，是并行 worktree 的固有问题。

还有一次，subagent 连续重试了同一个策略 6 次，全部失败，然后上报给我。我一看，其实是 spec 里有个边界条件没写清楚，agent 按 spec 实现了，但 spec 本身就是错的。这不是 agent 的问题，是我的问题。

我的结论是：subagent 可以在 2-3 小时内自主运行不跑偏，前提是：
1. SPEC 够精确（冷启动暴露的依赖分组问题就是反例）
2. task 边界够清晰（"实现校验器"比"完善反馈闭环"好得多）
3. worktree 之间没有依赖冲突（并行 task 最好真的能并行）

但即使满足了这些，你也得时不时看一眼。完全放手是不现实的。

## 四、Task 颗粒度

最优颗粒度大概是"一个 subagent 一次会话能完成，2-5 分钟的编码量"——PLAN 里就这么写的，实际做下来也差不多。这个粒度不是我一开始就想好的，是试出来的。刚开始我拆的 task 偏大，结果 subagent 经常跑偏或者做到一半超时。后来拆细了，反而完成率更高。

task 太小（比如"配 pyproject.toml"），启动 subagent 的开销（建 worktree、加载上下文、跑测试）比 task 本身还大。task 太大（比如"实现完整的反馈闭环"），subagent 一开始就容易偏，然后越走越远。我有个 task 拆得太大，subagent 做了 3 个小时还没完，中间已经偏离了原来的方向，最后我不得不取消让它重来。

我自己的判断标准是：一个 task 能不能用 3-5 个验收测试来定义？能，颗粒度就合适；不能，说明 task 太大，得拆。

## 五、SPEC / PLAN 质量的影响

最直接的例子是冷启动验证里的依赖分组问题。

SPEC §5.3 写了"可选/必选"依赖，但没落实到 pyproject.toml 的 extras 分组。冷启动 agent 在 T1.1 看到这个 spec 就卡住了——它不知道哪些依赖是 core 必选、哪些是 llm 可选、哪些是 memory 可选。它停下来问我，我才发现 spec 写得不精确。

后果是什么？如果冷启动没发现这个问题，正式实现会在 T1.1 卡住，然后我得花时间改 spec，再让 agent 重来。虽然只有一个问题，但它是 block 级别的——直接挡在项目启动的路上。

还有一个例子是 T3.3 修正策略引擎。PLAN 里写了"每种失败分类有对应的策略链"，但没写清楚"策略链用完怎么办"。subagent 实现的时候，默认在链的最后加了一个"上报人工"的兜底行为。这个决策是对的，但如果 PLAN 写得更清楚，它就不需要猜了。

教训是：SPEC 的概念层描述和实现层描述之间有 gap。概念层说"依赖是可选的"，实现层需要知道"哪个 extras 组包含哪些包"。概念层说"策略链"，实现层需要知道"链走完了怎么办"。只有把 spec 精确到"实现者不需要猜"的程度，agent 才能自己往前走。

## 六、最有效的策略

"把规则写进代码，而不是写进提示词。"

这不算 prompt 策略，更像架构策略——但在 harness 项目里比任何 prompt 技巧都管用。guardrail.check() 函数、分类器的规则引擎、策略引擎的状态机，都是确定性代码，不需要 LLM 理解"请小心"或"请检查"这类提示词就能工作。

提示词是软的，代码是硬的。能让代码做的事，不要交给提示词。这个道理我写 spec 的时候就知道，但真正做完了才切身体会到。分类器的规则引擎写了 100 行代码，就能处理 8 种失败类型，每种有优先级、有置信度计算——如果这些写在提示词里，LLM 不一定能执行对，而且每次执行结果都不一样。

## 七、凭据与分发让我想通了什么

凭据方面，我本来打算用环境变量搞定——"用户自己设 API_KEY=xxx 就行了"。但作业要求"首次运行引导用户安全录入 key"，这逼我实现了策略模式（钥匙串 + 加密文件）。虽然多写了代码，但现在回头看这才是对的：环境变量会在 shell history 里留痕迹，子进程也能读到，不安全。钥匙串方案虽然麻烦，但至少 key 不会随便泄露。

不过说实话，钥匙串在 Linux 上的体验不太好。D-Bus 服务得提前装好，否则 keyring 库会报错。我最后写了一个 fallback：钥匙串不可用时自动降级到加密文件。这个"降级"设计是在写代码的过程中才想到的，写 spec 的时候根本没考虑过"钥匙串装不上怎么办"。

分发方面，Dockerfile 的多阶段构建是写着写着才想清楚的——前端构建需要 node，运行只需要 Python，所以分两阶段能大幅减小镜像。如果只是随便打个包，我不会想到这个优化。另外，CI 的配置也让我想明白了一件事：测试要在 CI 里跑，意味着测试不能依赖网络和真实 LLM。这个约束反过来逼我用 MockLLMClient 写测试——结果反而让测试更可靠了。

## 八、如果重做

1. **WebUI 优先级降低。** WebUI 花了很多时间（FastAPI + React），但它的核心价值是"展示"而不是"功能"。如果重来，我会先做一个 CLI-only 版本，核心机制搞完整了再考虑 WebUI。其实现在回头看，WebUI 的很多功能 CLI 已经能做了，WebUI 只是换了一种展示方式。这个优先级的错误主要是我自己——做 WebUI 比做后端有趣，所以不自觉地把时间花在了前面。
2. **冷启动验证提前。** 这次冷启动是在 SPEC 和 PLAN 完成后做的。如果重来，我会在 SPEC 草案阶段就做一次快速冷启动——让另一个 agent 只读 SPEC，看它能不能理解，不用等 PLAN 写完。这样能在更早的阶段发现 spec 的问题，修复成本更低。
3. **减少 worktree 数量。** 每个 task 一个 worktree 的开销比想象中大。小 task（比如更新文档）直接 commit 到 main 更高效。worktree 应该留给有冲突风险的并行 task。另外，worktree 的命名也应该更规范——这次有些 worktree 自动生成的名字完全没有可读性，过两天就不知道哪个是哪个了。

## 九、对 Superpowers 的批判

Superpowers 的核心假设是"好的流程能产出好的结果"。这个假设大部分成立，但有几个地方我有点怀疑：

1. **流程的维护成本被低估了。** PLAN.md 的更新、AGENT_LOG.md 的记录、worktree 的管理——这些都在消耗本来可以用来写代码的时间。流程本身是好的，但边际收益递减。到项目后期，我经常在想要不要更新 PLAN.md：不更新吧，plan 和实际就对不上了；更新吧，改完 plan 天都黑了。这个问题在团队项目里可能更严重。

2. **TDD 强制执行在 AI 协作下有时会变成形式主义。** subagent 被要求"先写测试"，它写了一个测试，红了，然后写实现代码让它变绿——但测试本身可能有问题，或者测的是不重要的功能。TDD 的纪律被遵守了，但目的没达到。我遇到过的情况是：agent 写了一个测试，测的是"函数返回了字符串"，而不是"函数返回了正确的计算结果"。测试红了，agent 改实现让它返回了字符串，测试绿了——但功能还是错的。TDD 的"红"和"绿"本身不能保证正确性，只能保证一致性。

3. **Superpowers 假设了智能体足够可靠。** agent 严格按照 spec 执行的时候，流程很顺。但 agent 做出超出预期的行为时（比如擅自改 PLAN.md 格式），流程没有给出"怎么处理异常"的指导。这让我觉得 Superpowers 更适合确定性的代码生成，而不是探索性的设计工作。如果 task 是"写一个排序算法"，spec 写清楚了输入输出，agent 几乎不会出错。但如果 task 是"设计一个错误处理策略"，spec 写不清楚，agent 就容易跑偏。

4. **单人项目里的评审环节有点尴尬。** review 流程假设有另一个人做 code review，但单人项目里只能自己 review 自己生成的代码，或者再找 AI 做 review。这让我不太确定自己是不是真的发现了所有问题。我试过让 AI review AI 生成的代码，结果它经常说"代码质量良好，无需修改"——等于没 review。最后还是得自己一行一行看。

总的来说，Superpowers 是一套有用的脚手架。它守住了那些在 AI 协作中容易松懈的纪律——TDD、评审、计划。但它不能替你做判断。你仍然得自己回答"做什么"和"做对了吗"。这两件事，AI 暂时还帮不了。

这门课让我最深的体会是：AI 编码工具真正改变的不是写代码的速度，而是"写代码"和"想清楚"这两件事的分离。以前，写代码的过程本身就在帮你理清思路。现在，AI 可以把"写代码"做得很快，但前提是你得先想清楚。想不清楚的话，AI 只会帮你更快地写出错的代码。这个体会，可能比任何技术细节都重要。
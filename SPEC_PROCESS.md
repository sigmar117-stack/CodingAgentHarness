# CodingKit — SPEC_PROCESS 过程文档

> 本文档记录与 Superpowers 协作生成 SPEC 和 PLAN 的过程，以及冷启动验证的关键发现和修订。

---

## 一、Brainstorming 关键节点

### 1.1 智能体追问的好问题

在 brainstorming 过程中，以下问题让我们修正了原设想：

| 追问 | 我们的原设想 | 修正后 |
|------|------------|--------|
| "你说的反馈闭环，具体是指什么？是指测试失败后重试，还是更复杂的机制？" | 模糊地认为"就是让 agent 自己改代码" | 明确为四层机制：校验器 → 分类器 → 修正策略引擎 → 回灌器 |
| "修正策略做深，深在哪里？是分类更细还是决策逻辑更复杂？" | 没有区分"深"的方向 | 明确为方向 B（决策编排状态机）为主 + 方向 A（分类粒度）为辅 |
| "普通操作自动放行、危险操作拦截——这个'危险'的判定标准是什么？" | 笼统地认为"危险操作就是危险操作" | 明确 4 个危险工具 + 参数模式匹配规则 |
| "钥匙串和加密文件各有什么优劣？" | 没想过对比 | 产出对比表后决定两者都支持，策略模式 |
| "控制台日志和文件日志各有什么优劣？" | 没想过对比 | 决定两者都要：控制台人类可读，文件 JSON 结构化 |

### 1.2 AI 提出的、我们采纳的建议

| 建议 | 来源 | 采纳理由 |
|------|------|---------|
| 失败分类器采用优先级规则引擎（而非 ML 分类） | AI 提出 | 确定性、可单测、不依赖 LLM，符合 §A.4-C 的"移除 LLM 后可测"要求 |
| 修正策略引擎采用状态机模式 | AI 提出 | 天然适合"尝试→失败→换策略→上报"的流程，状态可观测 |
| 凭据存储采用策略模式（Strategy Pattern） | AI 提出 | 钥匙串和加密文件统一接口，用户可配置 |
| 日志采用 loguru 库同时处理控制台输出和文件 JSON 输出 | AI 提出 | 一个库两种输出，配置简单 |
| 危险动作拦截增加"修改后放行（m）"选项 | AI 提出 | 比单纯 y/n 更灵活，用户可修正命令而非简单否决 |

### 1.3 我们推翻或修正的 AI 建议

| 建议 | 推翻理由 |
|------|---------|
| "记忆系统建议使用 SQLite + embeddings 而非 ChromaDB" | 推翻。ChromaDB 更贴近"向量数据库"的语义，且纯 Python 实现无需额外服务，与 SPEC 的"可选依赖"定位一致 |
| "WebUI 建议使用 Next.js 全栈框架" | 推翻。太重，不符合"核心机制优先"原则，改为 FastAPI + React 轻量组合 |
| "修正策略建议加入 ML 模型预测成功率" | 推翻。不可单测、依赖数据训练，与 §A.4-C 冲突 |

---

## 二、冷启动验证记录

### 2.1 验证设置

| 项目 | 内容 |
|------|------|
| **主开发智能体** | Claude Code |
| **冷启动智能体** | 另一个 Claude Code 实例（全新 session，不导入历史） |
| **提供的材料** | 仅 `SPEC.md` + `PLAN.md` |
| **分配的 task** | 自主选择，完成 T1.1（项目脚手架）+ T1.2（凭据存储）+ T1.3（LLM 抽象层）+ T2.1（工具实现） |
| **指令** | "遇到不确定之处即暂停询问，而非凭猜测继续" |

### 2.2 暂停与提问记录

**提问点 #1：依赖打包策略**

> **问题**：`pyproject.toml` 的依赖怎么组织？PLAN T1.1 把所有依赖列成一坨，但 SPEC §5.3 和 T6.2 又说很多是可选的。chromadb 这类大依赖如果设成必选，`pip install -e .` 会拉几百 MB。
>
> **Agent 提出的方案**：
> 1. 拆分核心/可选（推荐）
> 2. 全部设为必选
> 3. 只装当前 task 所需

**分析**：
- **暴露的 SPEC 缺陷**：§5.3 虽然标注了"可选/必选"，但未给出 `pyproject.toml` 中具体的 `extras` 分组定义，导致实现者无法确定如何配置
- **是 SPEC 写错还是 Agent 读错**：**SPEC 写得不精确**。"可选"在 SPEC 中只是概念描述，没有落实到具体的 extras 分组名和包含关系
- **修订**：SPEC §5.3 重写为精确的依赖分组表（core/llm/memory/web/all/dev 六组），PLAN T1.1 同步更新

**提问点 #2：T1.3 适配器深度 + T2.1 run_tests 格式**

> **问题 A**：Claude/OpenAI 适配器要做到多深？无 API key 时真实调用没法集成测，但翻译逻辑能单测。
> 方案 1：完整工具调用翻译（推荐）
> 方案 2：先只做文本，工具调用推迟
> **决策**：方案 1。翻译逻辑是纯函数，可以用确定性单元测试覆盖，不需要 API key。

> **问题 B**：run_tests 工具用哪种 pytest 输出、返回什么？
> 方案 1：junitxml + 轻量结构（推荐，零新增依赖）
> 方案 2：加 json-report 依赖
> **决策**：方案 1。junitxml 是 pytest 内置的，run_tests 只负责结构化摘要，完整 FailureDetail 提取是 T3.1 校验器的职责。

**分析**：
- **暴露的 SPEC 缺陷**：这两个问题实际上 PLAN 已经写清楚了（T1.3 要求"支持 tool use/function calling"，T2.1 要求"返回结构化结果"），但 SPEC 没有明确 run_tests 和校验器的边界划分
- **是 SPEC 写错还是 Agent 读错**：**可以接受**——Agent 不是读错了，而是在确认实现深度，属于合理的谨慎，不算 SPEC 缺陷
- **修订**：无需修订 SPEC/PLAN，这两个决策已作为实现记录

### 2.3 产出与预期差距

| 维度 | 预期 | 实际 | 差距 |
|------|------|------|------|
| 依赖配置 | Agent 直接按 PLAN 执行 | Agent 卡住提问 | 中等——暴露了 SPEC 精确度不足 |
| 适配器深度 | Agent 直接按 PLAN 执行 | Agent 确认深度方案 | 小——合理谨慎，非 SPEC 缺陷 |
| run_tests 格式 | Agent 直接按 PLAN 执行 | Agent 确认输出格式 | 小——合理谨慎，非 SPEC 缺陷 |
| 任务完成度 | 预计能完成 1-2 个 task | **完成 4 个 task**（T1.1, T1.2, T1.3, T2.1） | 超出预期 |
| 代码质量 | 基础可用 | **51 个测试全部通过**，含 Mock 测试、边界覆盖 | 超出预期 |

### 2.4 修订记录

**修订 1：SPEC §5.3 — 依赖分组精确化**

修订前：笼统的"可选/必选"表格
修订后：精确的 extras 分组定义（core/llm/memory/web/all/dev），含每条安装命令

**修订 2：PLAN T1.1 — 实现要点精确化**

修订前：`"配置 pyproject.toml（依赖：typer, pydantic, httpx, ...）"`
修订后：明确列出每个 extras 组的依赖成员

**修订 3：无（验证性决策）**

T1.3 适配器深度和 T2.1 run_tests 格式的确认属于合理谨慎，不暴露 SPEC 缺陷，无需修订。但这两个决策已记入实现记录，确保后续层（T3.1 校验器、T4.1 agent 循环）的接口一致。

---

## 三、反思

### 3.1 Brainstorming 技能做得好的地方

1. **追问深度**：在"反馈闭环"和"修正策略"上追问了多轮，帮我们从模糊概念走到了可执行的机制设计
2. **结构化输出**：自动将讨论结果组织为 SPEC 的章节结构，减少了后期整理的工作量
3. **风险预判**：主动提出了 ChromaDB 复杂度、跨平台兼容性等我们没意识到的风险

### 3.2 Brainstorming 技能让我不满的地方

1. **依赖分组不够精确**：在 brainstorming 阶段讨论了"可选/必选"，但没有落实到 `pyproject.toml` 的 extras 定义级别——这直接导致了冷启动验证中的卡点
2. **对"可测试性"的验证不够**：虽然 SPEC 中写了"可独立单元测试"，但没有在 brainstorming 阶段要求每个机制都给出具体的 Mock 测试用例，导致部分机制的可测试性直到 T7.1 才被验证
3. **用户故事过于通用**：前几版用户故事和别的 harness 没有区别，直到追问"你的特点是什么"才修正

### 3.3 冷启动验证的教训

这次冷启动验证覆盖了 4 个 task（T1.1-T2.1），暴露了 1 个 SPEC 缺陷、确认了 2 个设计决策：

- **暴露的缺陷（1 个）**：依赖分组——SPEC 的"可选"概念没有落实到 `pyproject.toml` 的 extras 定义级别。如果没发现，会在 T1.1 实现时直接卡住
- **确认的决策（2 个）**：适配器深度选完整工具翻译、run_tests 用 junitxml 轻量结构——Agent 谨慎确认后直接推进，没有卡住
- **超出预期**：Agent 自主完成了 4 个 task（预期 1-2 个），51 个测试全部通过，代码质量高

核心教训：
- **SPEC 的精确度"够用"但不"够精确"**——概念层描述和实现层描述之间存在 gap，这个 gap 只有新 agent 才能发现
- **PLAN 的 task 描述需要更模板化**——涉及配置文件的 task 应直接给出代码片段，而非自然语言描述
- **冷启动验证的价值被充分验证**——只暴露一个问题，但这个问题是 block 级别的。如果没有冷启动验证，正式实现会在 T1.1 卡住
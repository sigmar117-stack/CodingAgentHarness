# Build and packaging configuration lives in pyproject.toml.
# This file is retained for compatibility with tooling that still reads setup.cfg
# and as the conventional location for future setuptools [options] overrides.
